build:
  docker:
    web: Dockerfile
    worker: Dockerfile

release:
  image: web
  command:
    - npx prisma migrate resolve --applied 20230726015824_wipe_test_data
    - npx prisma migrate deploy

run:
  web: npm start
  worker: npm run start-worker
