// import { StateCreator, create } from "zustand";

// interface CommonSlice {
//   loading: boolean;
// }

// const createCommonSlice: StateCreator<any, [], [], CommonSlice> = create(
//   (set) => ({
//     loading: false,
//     setLoading: (loading: boolean) => set({ loading: loading }),
//   }),
// );

// export { createCommonSlice, CommonSlice };
